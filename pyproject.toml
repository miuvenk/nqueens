[project]
name = "nqueens"
version = "0.1.0"
description = "N-Queens: CSP vs SA vs GA (NumPy-only)"
requires-python = ">=3.9"
dependencies = ["numpy>=2,<3"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
